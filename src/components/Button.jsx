import React from "react"
import styled from '@emotion/styled'

const Button = () => {
  return (
    <ButtonElement> rest</ButtonElement>
  )
};

const ButtonElement = styled("a")`

`

export default Button;